<Project>
  <Target Name="InitSubmodule" BeforeTargets="BeforeBuild">
    <Exec Command="git -C $(MSBuildThisFileDirectory)..\.. submodule update --init --recursive"
          WorkingDirectory="$(MSBuildThisFileDirectory)..\.."
          ContinueOnError="true" />
  </Target>
</Project>
