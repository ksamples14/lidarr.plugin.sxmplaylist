<?xml version="1.0" encoding="utf-8" ?>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Target Name="ILRepacker" AfterTargets="Build">
    <ItemGroup>
      <InputAssemblies Include="$(OutputPath)Lidarr.Plugin.XmPlaylist.dll" />
      <InputAssemblies Include="$(OutputPath)Lidarr.Common.dll" />
      <InputAssemblies Include="$(OutputPath)Lidarr.Core.dll" />
    </ItemGroup>

    <ILRepack
      Parallel="true"
      Internalize="true"
      InputAssemblies="@(InputAssemblies)"
      TargetKind="Dll"
      DebugInfo="true"
      LibraryPath="..\..\Submodules\Lidarr\_output\net8.0\;$(OutputPath)"
      OutputFile="$(OutputPath)$(AssemblyName).dll" />
  </Target>
</Project>
